spring.application.name=cruddemo

# Spring will automatically resolve this for us
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/student_tracker
spring.datasource.username=devuser
spring.datasource.password=06o}Q4L|

# Turn off spring boot banner
spring.main.banner-mode=off

# Reducing logging level to warn
logging.level.root=warn

# Add logging configs to display SQL statements
logging.level.org.hibernate.SQL=debug
logging.level.org.hibernate.orm.jdbc.bind=trace

# Configure JPA/Hibernate to drop & auto create the tables
spring.jpa.hibernate.ddl-auto=create
